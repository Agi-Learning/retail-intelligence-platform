spring:
  application:
    name: identity-01

app:
  api-base-path: /api/v1/identity/01

server:
  port: ${SERVER_PORT:8081}

management:
  endpoints:
    web:
      exposure:
        include: health,info
